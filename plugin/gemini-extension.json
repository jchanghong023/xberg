{
  "name": "xberg",
  "version": "1.2.8",
  "description": "Local document extraction: text, tables, metadata, images from 110 formats with optional OCR.",
  "contextFileName": "GEMINI.md",
  "mcpServers": {
    "xberg": {
      "command": "${extensionPath}/scripts/mcp-launch.sh",
      "args": [
        "mcp",
        "--transport",
        "stdio"
      ]
    }
  }
}
