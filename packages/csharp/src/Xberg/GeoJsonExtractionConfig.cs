// This file is auto-generated by alef — DO NOT EDIT.
// To regenerate: alef generate
// To verify freshness: alef verify
#nullable enable

using System;
using System.Collections.Generic;
using System.Text.Json;
using System.Text.Json.Serialization;

namespace Xberg;

/// <summary>
/// Configuration for GeoJSON extraction.
///
/// GeoJSON coordinates can dominate extraction output and duplicate large
/// geometry payloads in rendered content and metadata. The default therefore
/// emits a bounded aggregate summary. Set `Self.include_full_coordinates` only
/// when callers need every coordinate in the extracted text and accept output
/// proportional to the input.
/// </summary>
public sealed record GeoJsonExtractionConfig {
  /// <summary>
  /// Include every coordinate in rendered content and `flattened_fields`
  /// metadata.
  ///
  /// Defaults to `false`. When false, extraction reports feature, property,
  /// geometry, position-count, bounds, truncation, and discarded-category
  /// metadata and emits a `ProcessingWarning` for every GeoJSON input.
  /// </summary>
  [JsonPropertyName("include_full_coordinates")]
  public bool IncludeFullCoordinates { get; init; } = false;

  /// <summary>
  /// Parse a <see cref="GeoJsonExtractionConfig"/> from a JSON string.
  /// </summary>
  /// <exception cref="XbergException">When the JSON cannot be
  /// deserialised.</exception>
  public static GeoJsonExtractionConfig FromJson(string json) {
    try {
      return JsonSerializer.Deserialize<GeoJsonExtractionConfig>(json,
                                                                 JsonOptions) ??
             throw new XbergException(
                 $"Failed to parse GeoJsonExtractionConfig from JSON: deserializer returned null");
    } catch (XbergException) {
      throw;
    } catch (Exception e) {
      throw new XbergException(
          $"Failed to parse GeoJsonExtractionConfig from JSON: {e.Message}", e);
    }
  }

  private static readonly JsonSerializerOptions JsonOptions = new() {
    DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingDefault,
    Converters = { new JsonStringEnumConverter(JsonNamingPolicy.SnakeCaseLower),
                   new ByteArrayJsonConverter() },
  };

  /// <summary>Options for serializing config/input objects to FFI. Strips nulls
  /// (nullable C# fields default to null and would override required Rust
  /// fields with non-deserialisable nulls) but preserves explicit false/0 so
  /// caller intent is kept.</summary>
  private static readonly JsonSerializerOptions JsonSerializationOptions =
      new() {
        DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingNull,
        Converters = { new JsonStringEnumConverter(
                           JsonNamingPolicy.SnakeCaseLower),
                       new ByteArrayJsonConverter() },
      };
}
