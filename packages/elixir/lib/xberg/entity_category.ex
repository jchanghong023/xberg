# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:d066e8ced498566a2a4e2e652c34cc0fb6e3d672cf7e3966222cff14ad02a5b1
# To regenerate: alef generate
# To verify freshness: alef verify
defmodule Xberg.EntityCategory do
  @moduledoc """
  Standard entity categories produced by built-in NER backends.

  The `Custom(String)` variant lets caller-supplied categories (e.g. LLM
  schemas) flow through without losing fidelity to the consumer.
  """

  @typedoc "Standard entity categories produced by built-in NER backends."
  @type t :: term()

  @typedoc "A person's name."
  @type person :: :person
  @typedoc "A company, institution, or organisation name."
  @type organization :: :organization
  @typedoc "A geographic location (city, country, address)."
  @type location :: :location
  @typedoc "A calendar date."
  @type date :: :date
  @typedoc "A time of day or duration."
  @type time :: :time
  @typedoc "A monetary amount with optional currency."
  @type money :: :money
  @typedoc "A percentage value."
  @type percent :: :percent
  @typedoc "An email address."
  @type email :: :email
  @typedoc "A phone number."
  @type phone :: :phone
  @typedoc "A URL or URI."
  @type url :: :url
  @typedoc "A caller-supplied custom category label."
  @type custom :: %{type: :custom, custom: String.t()}

  @doc "Returns the serde wire value this variant carries on the JSON boundary (see `wire_variant_value` in alef's naming module) -- distinct from the atom's Elixir spelling."
  @spec wire_value(t()) :: String.t()
  def wire_value(:person), do: "person"
  def wire_value(:organization), do: "organization"
  def wire_value(:location), do: "location"
  def wire_value(:date), do: "date"
  def wire_value(:time), do: "time"
  def wire_value(:money), do: "money"
  def wire_value(:percent), do: "percent"
  def wire_value(:email), do: "email"
  def wire_value(:phone), do: "phone"
  def wire_value(:url), do: "url"
  def wire_value(:custom), do: "custom"
  def wire_value(value) when is_tuple(value), do: wire_value(elem(value, 0))
  def wire_value(value) when is_map(value), do: Map.fetch!(value, :type)

  defimpl String.Chars, for: Xberg.EntityCategory do
    @doc false
    def to_string(value), do: Xberg.EntityCategory.wire_value(value)
  end
end
