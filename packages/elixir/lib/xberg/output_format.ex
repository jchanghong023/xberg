# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:a0400502f22e2b05be0672af4f2b22d38044b837e4f931e96cdfc842c172cc96
# To regenerate: alef generate
# To verify freshness: alef verify
defmodule Xberg.OutputFormat do
  @moduledoc """
  Output format for extraction results.

  Controls the format of the `content` field in `ExtractedDocument`.
  When set to `Markdown`, `Djot`, or `Html`, the output uses that format.
  `Plain` returns the raw extracted text.
  """

  @typedoc "Output format for extraction results."
  @type t :: term()

  @typedoc "Plain text content only (default)"
  @type plain :: :plain
  @typedoc "Markdown format"
  @type markdown :: :markdown
  @typedoc "Djot markup format"
  @type djot :: :djot
  @typedoc "HTML format"
  @type html :: :html
  @typedoc "JSON tree format with heading-driven sections."
  @type json :: :json
  @typedoc "Docling DocTags format (tables rendered as OTSL)."
  @type doc_tags :: :doc_tags
  @typedoc "Custom renderer registered via the RendererRegistry. The string is the renderer name (e.g., \"docx\", \"latex\")."
  @type custom :: %{type: :custom, custom: String.t()}

  @doc "Returns the serde wire value this variant carries on the JSON boundary (see `wire_variant_value` in alef's naming module) -- distinct from the atom's Elixir spelling."
  @spec wire_value(t()) :: String.t()
  def wire_value(:plain), do: "plain"
  def wire_value(:markdown), do: "markdown"
  def wire_value(:djot), do: "djot"
  def wire_value(:html), do: "html"
  def wire_value(:json), do: "json"
  def wire_value(:doc_tags), do: "doctags"
  def wire_value(:custom), do: "custom"
  def wire_value(value) when is_tuple(value), do: wire_value(elem(value, 0))
  def wire_value(value) when is_map(value), do: Map.fetch!(value, :type)

  defimpl String.Chars, for: Xberg.OutputFormat do
    @doc false
    def to_string(value), do: Xberg.OutputFormat.wire_value(value)
  end
end
