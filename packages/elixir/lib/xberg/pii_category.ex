# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:cdce1118a2a60951999ad3eb3fb5a077ad82afc2b1a1c36efc0d1cf7604b9859
# To regenerate: alef generate
# To verify freshness: alef verify
defmodule Xberg.PiiCategory do
  @moduledoc "PII categories the pattern engine recognises."

  @typedoc "PII categories the pattern engine recognises."
  @type t :: term()

  @typedoc "Email address (e.g. `user@example.com`)."
  @type email :: :email
  @typedoc "Phone number in any common format."
  @type phone :: :phone
  @typedoc "US Social Security Number."
  @type ssn :: :ssn
  @typedoc "Payment card number (Visa, Mastercard, Amex, etc.)."
  @type credit_card :: :credit_card
  @typedoc "Postal / ZIP code."
  @type postal_code :: :postal_code
  @typedoc "IPv4 or IPv6 address."
  @type ip_address :: :ip_address
  @typedoc "International Bank Account Number."
  @type iban :: :iban
  @typedoc "SWIFT / BIC bank identifier code."
  @type swift_bic :: :swift_bic
  @typedoc "Date of birth."
  @type date_of_birth :: :date_of_birth
  @typedoc "Person name, surfaced by the optional NER backend."
  @type person :: :person
  @typedoc "Organization name, surfaced by the optional NER backend."
  @type organization :: :organization
  @typedoc "Location, surfaced by the optional NER backend."
  @type location :: :location
  @typedoc "Caller-supplied custom category (e.g. internal employee IDs)."
  @type custom :: %{type: :custom, custom: String.t()}

  @doc "Returns the serde wire value this variant carries on the JSON boundary (see `wire_variant_value` in alef's naming module) -- distinct from the atom's Elixir spelling."
  @spec wire_value(t()) :: String.t()
  def wire_value(:email), do: "email"
  def wire_value(:phone), do: "phone"
  def wire_value(:ssn), do: "ssn"
  def wire_value(:credit_card), do: "credit_card"
  def wire_value(:postal_code), do: "postal_code"
  def wire_value(:ip_address), do: "ip_address"
  def wire_value(:iban), do: "iban"
  def wire_value(:swift_bic), do: "swift_bic"
  def wire_value(:date_of_birth), do: "date_of_birth"
  def wire_value(:person), do: "person"
  def wire_value(:organization), do: "organization"
  def wire_value(:location), do: "location"
  def wire_value(:custom), do: "custom"
  def wire_value(value) when is_tuple(value), do: wire_value(elem(value, 0))
  def wire_value(value) when is_map(value), do: Map.fetch!(value, :type)

  defimpl String.Chars, for: Xberg.PiiCategory do
    @doc false
    def to_string(value), do: Xberg.PiiCategory.wire_value(value)
  end
end
