// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:bda39134af91c96f4d54c25ea81bdc45fa31971105f77d42253455777bda67b1
// To regenerate: alef generate
// To verify freshness: alef verify
package io.xberg;

import java.util.List;
@SuppressWarnings("PMD")
public final class Xberg {
  private Xberg() {}

  /**
   * Classify a document's chunks and return the updated document.
   *
   * This owned form preserves the mutations when the document crosses a
   * language-binding boundary. Rust callers that already own a mutable document
   * can use classify_chunks to avoid moving it.
   * {@literal @}throws XbergRsException Returns the same validation and LLM
   * errors as classify_chunks.
   */
  public static ExtractedDocument
  classifyChunksOwned(final ExtractedDocument result,
                      final ChunkClassificationConfig config)
      throws XbergRsException {
    java.util.Objects.requireNonNull(result, "result must not be null");
    java.util.Objects.requireNonNull(config, "config must not be null");
    return XbergRs.classifyChunksOwned(result, config);
  }

  /**
   * Probe the backends and settings in {@code config} and report what will
   * actually execute on this host.
   *
   * Runs no downloads and no billable API calls. Backends that are not compiled
   * in or whose models are not cached report {@code Skip} rather than failing.
   */
  public static DoctorReport doctor(final ExtractionConfig config)
      throws XbergRsException {
    java.util.Objects.requireNonNull(config, "config must not be null");
    return XbergRs.doctor(config);
  }

  /**
   * Extract content from a single bytes or URI input.
   */
  public static ExtractionResult extract(final ExtractInput input,
                                         final ExtractionConfig config)
      throws XbergRsException {
    java.util.Objects.requireNonNull(input, "input must not be null");
    java.util.Objects.requireNonNull(config, "config must not be null");
    return XbergRs.extract(input, config);
  }

  /**
   * Extract content from multiple bytes or URI inputs.
   */
  public static ExtractionResult extractBatch(final List<ExtractInput> inputs,
                                              final ExtractionConfig config)
      throws XbergRsException {
    java.util.Objects.requireNonNull(inputs, "inputs must not be null");
    java.util.Objects.requireNonNull(config, "config must not be null");
    return XbergRs.extractBatch(inputs, config);
  }

  /**
   * Find unmarked claims in markdown text.
   *
   * Returns lines that assert a claim but carry neither a footnote citation
   * anchor ({@code [^...]}) nor an inference marker ({@code [*inference*]}).
   *
   * The heuristic is simple: a line that contains alphabetic words, ends with
   * sentence punctuation, and is not a heading, blank line, or markup-only line
   * is considered a claim. Exclude lines that appear in the citation block
   * (after {@code ---} + {@code &lt;!-- citations ... --&gt;}).
   * {@literal @}param markdown The markdown text to search
   *
   * {@literal @}return A vector of trimmed line text strings for unmarked
   * claims.
   */
  public static List<String> findUnmarkedClaims(final String markdown)
      throws XbergRsException {
    java.util.Objects.requireNonNull(markdown, "markdown must not be null");
    return XbergRs.findUnmarkedClaims(markdown);
  }

  /**
   * List names of all registered document extractors.
   */
  public static List<String> listDocumentExtractors() throws XbergRsException {
    return XbergRs.listDocumentExtractors();
  }

  /**
   * List the names of all registered embedding backends.
   *
   * Used by {@code xberg-cli}, the api/mcp endpoints, and generated language
   * bindings.
   */
  public static List<String> listEmbeddingBackends() throws XbergRsException {
    return XbergRs.listEmbeddingBackends();
  }

  /**
   * List every registered OCR backend's name alongside its declared supported
   * languages.
   *
   * This is the capability-enumeration counterpart to list_ocr_backends: where
   * that function exposes only backend names, this exposes each backend's
   * {@code supported_languages()} too, so a consumer (for example, a
   * job-acceptance gate) does not need to hardcode a second list of backend
   * languages.
   *
   * # Determinism
   *
   * The returned vector is sorted by {@code name}, regardless of registration
   * order or the order reported by the underlying registry. {@code
   * supported_languages} within each entry is **not** sorted — see
   * OcrBackendCapabilities.supported_languages for why.
   *
   * # Cost
   *
   * Calling this is not free for every backend. In particular, {@code
   * TesseractBackend}'s
   * {@code supported_languages()} allocates a Tesseract API and initializes it
   * against the same tessdata directory a real OCR job resolves ({@code
   * resolve_tessdata_path}), the first time it is called, to enumerate
   * installed tessdata languages; subsequent calls are served from a cache.
   * {@literal @}throws XbergRsException Returns an error only if the registry
   * lock cannot be acquired in the current environment.
   */
  public static List<OcrBackendCapabilities> listOcrBackendCapabilities()
      throws XbergRsException {
    return XbergRs.listOcrBackendCapabilities();
  }

  /**
   * List all registered OCR backends.
   *
   * Returns the names of all OCR backends currently registered in the global
   * registry.
   * {@literal @}return A vector of OCR backend names.
   */
  public static List<String> listOcrBackends() throws XbergRsException {
    return XbergRs.listOcrBackends();
  }

  /**
   * List all registered post-processor names.
   *
   * Returns a vector of all post-processor names currently registered in the
   * global registry.
   * {@literal @}return - {@code Ok(Vec&lt;String&gt;)} - Vector of
   * post-processor names
   * - {@code Err(...)} if the registry lock is poisoned
   */
  public static List<String> listPostProcessors() throws XbergRsException {
    return XbergRs.listPostProcessors();
  }

  /**
   * List names of all registered renderers.
   * {@literal @}throws XbergRsException Returns an error if the registry lock
   * is poisoned.
   */
  public static List<String> listRenderers() throws XbergRsException {
    return XbergRs.listRenderers();
  }

  /**
   * List the names of all registered reranker backends.
   *
   * Used by {@code xberg-cli}, the api/mcp endpoints, and generated language
   * bindings.
   */
  public static List<String> listRerankerBackends() throws XbergRsException {
    return XbergRs.listRerankerBackends();
  }

  /**
   * List all supported document formats.
   *
   * Returns every file extension Xberg recognizes together with its
   * corresponding MIME type, derived from the central format registry.
   * Formats that have no registered file extension (such as source code,
   * which is detected dynamically) are not included.
   *
   * The static {@code EXT_TO_MIME} table lists every format the *codebase*
   * knows how to describe, regardless of which Cargo features were compiled in.
   * Advertising that table directly would claim support for extractors that may
   * not exist in this build (see GH#1387). To keep the advertised catalogue
   * honest, the table is intersected with the document extractor registry: an
   * extension is only included if some registered extractor actually claims its
   * MIME type in this build. This can never drift from reality and
   * automatically covers third-party extractors registered at runtime.
   *
   * The list is sorted alphabetically by file extension.
   * {@literal @}return A vector of SupportedFormat entries sorted by extension,
   * limited to formats with a registered extractor in this build.
   */
  public static List<SupportedFormat> listSupportedFormats()
      throws XbergRsException {
    return XbergRs.listSupportedFormats();
  }

  /**
   * List the names of all registered tokenizer backends.
   *
   * Used by {@code xberg-cli}, the api/mcp endpoints, and generated language
   * bindings.
   */
  public static List<String> listTokenizerBackends() throws XbergRsException {
    return XbergRs.listTokenizerBackends();
  }

  /**
   * List names of all registered validators.
   */
  public static List<String> listValidators() throws XbergRsException {
    return XbergRs.listValidators();
  }

  /**
   * Discover all pages and sitemaps reachable from {@code uri} without
   * extracting document content.
   *
   * Builds a crawlberg.CrawlEngine from {@code config.crawl}, calls
   * CrawlEngine.map, and returns the set of discovered URLs as a
   * crawlberg.MapResult (re-exported as {@code MapResult}).
   *
   * Use this when you need the URL inventory of a site before committing to
   * full document extraction — e.g. to build a crawl queue or validate scope.
   * {@literal @}throws XbergRsException Returns {@code Validation} if the crawl
   * configuration fails validation or if the map operation itself fails.
   */
  public static MapResult mapUrl(final String uri,
                                 final UrlExtractionConfig config)
      throws XbergRsException {
    java.util.Objects.requireNonNull(uri, "uri must not be null");
    java.util.Objects.requireNonNull(config, "config must not be null");
    return XbergRs.mapUrl(uri, config);
  }

  /**
   * Rank a set of documents against a query by MaxSim score, descending.
   *
   * Mirrors the sort/truncate shape of {@code crate.reranking}'s {@code
   * build_results}, minus top-k truncation (callers slice the returned {@code
   * Vec} themselves).
   *
   * Pure CPU primitive — available without ONNX Runtime.
   */
  public static List<LateInteractionMatch>
  maxSimRank(final MultiVectorEmbedding query,
             final List<MultiVectorEmbedding> docs) throws XbergRsException {
    java.util.Objects.requireNonNull(query, "query must not be null");
    java.util.Objects.requireNonNull(docs, "docs must not be null");
    return XbergRs.maxSimRank(query, docs);
  }

  /**
   * Score a query against a document using ColBERT's MaxSim operator: for each
   * query token vector, take the maximum dot product against any document
   * token vector, then sum across query tokens.
   *
   * Returns {@code 0.0} if {@code query} and {@code doc} have mismatched
   * dimensionality, if either has zero tokens, or if either is not well-formed
   * per MultiVectorEmbedding.is_well_formed (its {@code data} length does not
   * match
   * {@code num_tokens * dim}).
   *
   * Pure CPU primitive — available without ONNX Runtime.
   */
  public static double maxSimScore(final MultiVectorEmbedding query,
                                   final MultiVectorEmbedding doc)
      throws XbergRsException {
    java.util.Objects.requireNonNull(query, "query must not be null");
    java.util.Objects.requireNonNull(doc, "doc must not be null");
    return XbergRs.maxSimScore(query, doc);
  }

  /**
   * Check whether a specific registered OCR backend supports a language.
   *
   * Delegates to the named backend's own OcrBackend.supports_language, which is
   * the correct per-language decision — do not infer support (or its absence)
   * from whether list_ocr_backend_capabilities reports an empty {@code
   * supported_languages} list for that backend, since an empty list can mean
   * "does not enumerate" rather than "supports nothing" (see
   * OcrBackendCapabilities.supported_languages).
   * {@literal @}param backend Name of a registered OCR backend, as returned by
   * list_ocr_backends. Lookup is case-insensitive and resolves the same {@code
   * paddleocr} alias as backend dispatch.
   *
   * {@literal @}param language Language code to check (e.g. {@code "eng"},
   * {@code "deu"}).
   *
   * {@literal @}throws XbergRsException Returns an error if no backend with
   * that name (or alias) is registered.
   */
  public static boolean ocrBackendSupportsLanguage(final String backend,
                                                   final String language)
      throws XbergRsException {
    java.util.Objects.requireNonNull(backend, "backend must not be null");
    java.util.Objects.requireNonNull(language, "language must not be null");
    return XbergRs.ocrBackendSupportsLanguage(backend, language);
  }

  /**
   * Verify that an excerpt appears verbatim in source text.
   *
   * Performs exact matching by default. Also tries whitespace-normalized
   * matching (collapsing runs of whitespace on both sides) since PDF-extracted
   * text often has irregular spacing.
   * {@literal @}param excerpt The text snippet to find
   *
   * {@literal @}param sourceText The full source text to search
   *
   * {@literal @}return {@code true} if the excerpt appears (exactly or with
   * normalized whitespace), {@code false} otherwise.
   */
  public static boolean verifyExcerpt(final String excerpt,
                                      final String sourceText)
      throws XbergRsException {
    java.util.Objects.requireNonNull(excerpt, "excerpt must not be null");
    java.util.Objects.requireNonNull(sourceText, "sourceText must not be null");
    return XbergRs.verifyExcerpt(excerpt, sourceText);
  }
  public static void registerOcrBackend(final IOcrBackend impl)
      throws XbergRsException {
    try {
      OcrBackendBridge.registerOcrBackend(impl);
    } catch (Exception e) {
      throw new XbergRsException(e.getMessage(), e);
    }
  }

  public static void unregisterOcrBackend(final String name)
      throws XbergRsException {
    try {
      OcrBackendBridge.unregisterOcrBackend(name);
    } catch (Exception e) {
      throw new XbergRsException(e.getMessage(), e);
    }
  }

  public static void clearOcrBackends() throws XbergRsException {
    try {
      OcrBackendBridge.clearOcrBackends();
    } catch (Exception e) {
      throw new XbergRsException(e.getMessage(), e);
    }
  }

  public static void registerPostProcessor(final IPostProcessor impl)
      throws XbergRsException {
    try {
      PostProcessorBridge.registerPostProcessor(impl);
    } catch (Exception e) {
      throw new XbergRsException(e.getMessage(), e);
    }
  }

  public static void unregisterPostProcessor(final String name)
      throws XbergRsException {
    try {
      PostProcessorBridge.unregisterPostProcessor(name);
    } catch (Exception e) {
      throw new XbergRsException(e.getMessage(), e);
    }
  }

  public static void clearPostProcessors() throws XbergRsException {
    try {
      PostProcessorBridge.clearPostProcessors();
    } catch (Exception e) {
      throw new XbergRsException(e.getMessage(), e);
    }
  }

  public static void registerValidator(final IValidator impl)
      throws XbergRsException {
    try {
      ValidatorBridge.registerValidator(impl);
    } catch (Exception e) {
      throw new XbergRsException(e.getMessage(), e);
    }
  }

  public static void unregisterValidator(final String name)
      throws XbergRsException {
    try {
      ValidatorBridge.unregisterValidator(name);
    } catch (Exception e) {
      throw new XbergRsException(e.getMessage(), e);
    }
  }

  public static void clearValidators() throws XbergRsException {
    try {
      ValidatorBridge.clearValidators();
    } catch (Exception e) {
      throw new XbergRsException(e.getMessage(), e);
    }
  }

  public static void registerDocumentExtractor(final IDocumentExtractor impl)
      throws XbergRsException {
    try {
      DocumentExtractorBridge.registerDocumentExtractor(impl);
    } catch (Exception e) {
      throw new XbergRsException(e.getMessage(), e);
    }
  }

  public static void unregisterDocumentExtractor(final String name)
      throws XbergRsException {
    try {
      DocumentExtractorBridge.unregisterDocumentExtractor(name);
    } catch (Exception e) {
      throw new XbergRsException(e.getMessage(), e);
    }
  }

  public static void clearDocumentExtractors() throws XbergRsException {
    try {
      DocumentExtractorBridge.clearDocumentExtractors();
    } catch (Exception e) {
      throw new XbergRsException(e.getMessage(), e);
    }
  }

  public static void registerEmbeddingBackend(final IEmbeddingBackend impl)
      throws XbergRsException {
    try {
      EmbeddingBackendBridge.registerEmbeddingBackend(impl);
    } catch (Exception e) {
      throw new XbergRsException(e.getMessage(), e);
    }
  }

  public static void unregisterEmbeddingBackend(final String name)
      throws XbergRsException {
    try {
      EmbeddingBackendBridge.unregisterEmbeddingBackend(name);
    } catch (Exception e) {
      throw new XbergRsException(e.getMessage(), e);
    }
  }

  public static void clearEmbeddingBackends() throws XbergRsException {
    try {
      EmbeddingBackendBridge.clearEmbeddingBackends();
    } catch (Exception e) {
      throw new XbergRsException(e.getMessage(), e);
    }
  }

  public static void registerRenderer(final IRenderer impl)
      throws XbergRsException {
    try {
      RendererBridge.registerRenderer(impl);
    } catch (Exception e) {
      throw new XbergRsException(e.getMessage(), e);
    }
  }

  public static void unregisterRenderer(final String name)
      throws XbergRsException {
    try {
      RendererBridge.unregisterRenderer(name);
    } catch (Exception e) {
      throw new XbergRsException(e.getMessage(), e);
    }
  }

  public static void clearRenderers() throws XbergRsException {
    try {
      RendererBridge.clearRenderers();
    } catch (Exception e) {
      throw new XbergRsException(e.getMessage(), e);
    }
  }

  public static void registerRerankerBackend(final IRerankerBackend impl)
      throws XbergRsException {
    try {
      RerankerBackendBridge.registerRerankerBackend(impl);
    } catch (Exception e) {
      throw new XbergRsException(e.getMessage(), e);
    }
  }

  public static void unregisterRerankerBackend(final String name)
      throws XbergRsException {
    try {
      RerankerBackendBridge.unregisterRerankerBackend(name);
    } catch (Exception e) {
      throw new XbergRsException(e.getMessage(), e);
    }
  }

  public static void clearRerankerBackends() throws XbergRsException {
    try {
      RerankerBackendBridge.clearRerankerBackends();
    } catch (Exception e) {
      throw new XbergRsException(e.getMessage(), e);
    }
  }

  public static void registerTokenizerBackend(final ITokenizerBackend impl)
      throws XbergRsException {
    try {
      TokenizerBackendBridge.registerTokenizerBackend(impl);
    } catch (Exception e) {
      throw new XbergRsException(e.getMessage(), e);
    }
  }

  public static void unregisterTokenizerBackend(final String name)
      throws XbergRsException {
    try {
      TokenizerBackendBridge.unregisterTokenizerBackend(name);
    } catch (Exception e) {
      throw new XbergRsException(e.getMessage(), e);
    }
  }

  public static void clearTokenizerBackends() throws XbergRsException {
    try {
      TokenizerBackendBridge.clearTokenizerBackends();
    } catch (Exception e) {
      throw new XbergRsException(e.getMessage(), e);
    }
  }
}
