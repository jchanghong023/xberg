// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:79dd360cabb8d2e7cbace999be9b41929da69923eb2f77568fc4ddcecaf14e52
// To regenerate: alef generate
// To verify freshness: alef verify
package io.xberg;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;

/**
 * PII categories the pattern engine recognises.
 */
@SuppressWarnings("PMD")
public enum PiiCategory {
  /**
   * Email address (e.g. {@code user@example.com}).
   */
  EMAIL("email"),
  /**
   * Phone number in any common format.
   */
  PHONE("phone"),
  /**
   * US Social Security Number.
   */
  SSN("ssn"),
  /**
   * Payment card number (Visa, Mastercard, Amex, etc.).
   */
  CREDIT_CARD("credit_card"),
  /**
   * Postal / ZIP code.
   */
  POSTAL_CODE("postal_code"),
  /**
   * IPv4 or IPv6 address.
   */
  IP_ADDRESS("ip_address"),
  /**
   * International Bank Account Number.
   */
  IBAN("iban"),
  /**
   * SWIFT / BIC bank identifier code.
   */
  SWIFT_BIC("swift_bic"),
  /**
   * Date of birth.
   */
  DATE_OF_BIRTH("date_of_birth"),
  /**
   * Person name, surfaced by the optional NER backend.
   */
  PERSON("person"),
  /**
   * Organization name, surfaced by the optional NER backend.
   */
  ORGANIZATION("organization"),
  /**
   * Location, surfaced by the optional NER backend.
   */
  LOCATION("location"),
  /**
   * Caller-supplied custom category (e.g. internal employee IDs).
   *
   * Surfaced by the redaction engine when a hit comes from
   * RedactionConfig.custom_terms(crate.core.config.redaction.RedactionConfig.custom_terms)
   * or
   * RedactionConfig.custom_patterns(crate.core.config.redaction.RedactionConfig.custom_patterns).
   * The string is the label passed alongside the term/pattern. Use those
   * fields rather than constructing {@code Custom} directly via the
   * {@code categories} filter — the pattern engine cannot detect arbitrary text
   * from a category name alone.
   */
  CUSTOM("custom");

  /** The string value. */
  private final String value;

  PiiCategory(final String value) { this.value = value; }

  /** Returns the string value. */
  @JsonValue
  public String getValue() {
    return value;
  }

  /** Creates an instance from a string value. */
  @JsonCreator
  public static PiiCategory fromValue(final String value) {
    for (PiiCategory e : values()) {
      if (e.value.equalsIgnoreCase(value)) {
        return e;
      }
    }
    throw new IllegalArgumentException("Unknown PiiCategory value: " + value);
  }

  /** Returns the wire-format string value (matches JSON serialization). */
  @Override
  public String toString() {
    return value;
  }
}
